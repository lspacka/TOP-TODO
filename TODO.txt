
+ fix local storage not updating after deleting a project. also, just noticed a weird bug 
  where its not deleting the last one(s), but after deleting the first one, then it works normally.
- the new task modal auto filling. fix it by emptying it only if its a new project
+ fix clear all pros
- fix add to important bug
- fix date lists bug
- update local storage when closing, also set a currentPro item to show the page on loading as it was when it was closed
- add wojak pic
- update important icon on all the lists the task is in, not just 'important'
- add key actions to modals
